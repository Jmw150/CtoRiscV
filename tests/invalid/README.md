# Invalid Programs

These programs are intended to be rejected once the compiler has a dedicated
invalid-test harness and clearer diagnostics.

For now they serve as specification examples for parser/type/scope errors that
the compiler should eventually report intentionally rather than crashing.
